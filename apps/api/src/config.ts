export type Environment = "development" | "test" | "production";
export interface MorokConfig {nodeEnv:Environment;host:string;port:number;corsOrigin:string;mongodbUri:string;mongodbDatabase:string;logLevel:string;modelApiUrl?:string;modelApiKey?:string;modelName:string;modelTemperature:number;webTimeoutMs:number;maxFileBytes:number;vaultKey?:string;}
export function loadConfig(env:NodeJS.ProcessEnv=process.env):MorokConfig{const nodeEnv=(env.NODE_ENV??"development") as Environment;const port=Number(env.PORT??10000);if(!Number.isInteger(port)||port<=0)throw new Error("PORT must be a positive integer");if(!env.MONGODB_URI)throw new Error("MONGODB_URI is required");const modelTemperature=Number(env.MODEL_TEMPERATURE??0.4);const webTimeoutMs=Number(env.WEB_TIMEOUT_MS??10000);const maxFileBytes=Number(env.MAX_FILE_BYTES??10000000);if(!Number.isFinite(modelTemperature)||modelTemperature<0||modelTemperature>2)throw new Error("MODEL_TEMPERATURE must be between 0 and 2");if(!Number.isInteger(webTimeoutMs)||webTimeoutMs<1000)throw new Error("WEB_TIMEOUT_MS must be at least 1000");if(!Number.isInteger(maxFileBytes)||maxFileBytes<=0)throw new Error("MAX_FILE_BYTES must be positive");return{nodeEnv,host:env.HOST??"0.0.0.0",port,corsOrigin:env.CORS_ORIGIN??"*",mongodbUri:env.MONGODB_URI,mongodbDatabase:env.MONGODB_DATABASE??"morok",logLevel:env.LOG_LEVEL??"info",modelApiUrl:env.MODEL_API_URL||undefined,modelApiKey:env.MODEL_API_KEY||undefined,modelName:env.MODEL_NAME??"morok",modelTemperature,webTimeoutMs,maxFileBytes,vaultKey:env.VAULT_KEY||undefined};}
export const config=loadConfig();