import {randomUUID} from "node:crypto"; import type {MorokEvent,MorokEventName} from "./types.js"; type Handler=(event:MorokEvent)=>void|Promise<void>; export class EventBus{private handlers=new Map<MorokEventName,Set<Handler>>(); on(n:MorokEventName,h:Handler){const s=this.handlers.get(n)??new Set<Handler>();s.add(h);this.handlers.set(n,s);return()=>s.delete(h)} async emit<T>(name:MorokEventName,payload:T){const e={id:randomUUID(),name,timestamp:new Date().toISOString(),payload} as MorokEvent<T>;for(const h of this.handlers.get(name)??[])await h(e);return e}}