import React,{useEffect,useState}from"react";import{createRoot}from"react-dom/client";import"./styles.css";const API=import.meta.env.VITE_API_URL??"http://localhost:10000";function App(){const[status,setStatus]=useState("Verificando...");const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);useEffect(()=>{fetch(API+"/health").then(r=>r.ok?setStatus("Online"):setStatus("API indisponível")).catch(()=>setStatus("API indisponível"))},[]);async function send(){if(!message.trim())return;setLoading(true);setMessage("");await new Promise(r=>setTimeout(r,200));setLoading(false)}return <main><header><strong>MOROK</strong><span>{status}</span></header><section><div className="conversation"><p>Olá. Eu sou o Morok.</p><p>Fundação operacional carregada.</p></div><form onSubmit={e=>{e.preventDefault();void send()}}><input value={message} onChange={e=>setMessage(e.target.value)} placeholder="Digite um comando..." disabled={loading}/><button disabled={loading}>{loading?"Processando...":"Enviar"}</button></form></section></main>}createRoot(document.getElementById("root")!).render(<React.StrictMode><App/></React.StrictMode>);